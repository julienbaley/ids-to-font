# Odes IDS grid request

Please create a grid image showing all 218 IDS expressions listed in
`scripts/odes-ids-218.txt`. Use dashed lacunae and BabelStone Han as the
matching font.

`{?}` is not included because it is a special tofu request rather than an IDS
expression.
